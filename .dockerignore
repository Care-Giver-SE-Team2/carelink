**/node_modules
**/target
**/dist
**/coverage
.git
.github
# Design documents are not build inputs, except the OpenAPI contract, which the
# backend build copies onto the classpath (see Dockerfile and backend/pom.xml).
docs/**
!docs/api/openapi.yaml
*.md
zap-out
reports
