/**
 * Domain model of the visit module (the visit lifecycle: assignment, state transitions, tasks, vitals, evidence, elder confirmation): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.visit.domain.model;
