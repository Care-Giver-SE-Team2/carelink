/**
 * Implementations of the domain repository ports for the visit module, plus the
 * mappers between JPA entities and domain objects. identity's AppUserMapper and
 * AppUserRepositoryAdapter are the template.
 */
package sg.nus.carelink.visit.infrastructure.persistence.adapter;
