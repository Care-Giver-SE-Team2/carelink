/**
 * Presentation layer of the incident module: REST controllers and their request/response DTOs.
 * Talks to the application layer only; it never reaches a repository implementation
 * (ArchUnit enforces this).
 */
package sg.nus.carelink.incident.controller;
