/**
 * Domain model of the incident module (incidents, their escalation log, family acknowledgement and spot checks): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.incident.domain.model;
