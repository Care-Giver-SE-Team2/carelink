/**
 * Presentation layer of the notification module: REST controllers and their request/response DTOs.
 * Talks to the application layer only; it never reaches a repository implementation
 * (ArchUnit enforces this).
 */
package sg.nus.carelink.notification.controller;
