/**
 * Implementations of the domain repository ports for the profile module, plus the
 * mappers between JPA entities and domain objects. identity's AppUserMapper and
 * AppUserRepositoryAdapter are the template.
 */
package sg.nus.carelink.profile.infrastructure.persistence.adapter;
