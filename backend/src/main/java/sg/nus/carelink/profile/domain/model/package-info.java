/**
 * Domain model of the profile module (elders, caregivers, family members, their bindings, intake applications and credentials): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.profile.domain.model;
