/**
 * Application layer of the profile module: one service method per use case. Orchestrates
 * the domain model and the domain repository ports; holds no business rules itself.
 */
package sg.nus.carelink.profile.application;
