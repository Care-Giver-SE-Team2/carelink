/**
 * Domain model of the rostering module (caregiver availability and absences, rostering runs, candidates and constraint checks): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.rostering.domain.model;
