/**
 * Implementations of the domain repository ports for the report module, plus the
 * mappers between JPA entities and domain objects. identity's AppUserMapper and
 * AppUserRepositoryAdapter are the template.
 */
package sg.nus.carelink.report.infrastructure.persistence.adapter;
