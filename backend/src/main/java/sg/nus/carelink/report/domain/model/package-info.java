/**
 * Domain model of the report module (periodic reports, value-added services and requests, caregiver reviews): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.report.domain.model;
