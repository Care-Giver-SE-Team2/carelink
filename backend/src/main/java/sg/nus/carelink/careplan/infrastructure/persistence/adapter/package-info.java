/**
 * Implementations of the domain repository ports for the careplan module, plus the
 * mappers between JPA entities and domain objects. identity's AppUserMapper and
 * AppUserRepositoryAdapter are the template.
 */
package sg.nus.carelink.careplan.infrastructure.persistence.adapter;
