/**
 * Domain model of the careplan module (care plans, the plan node tree and the credentials a plan requires): plain Java with the business rules and the
 * design patterns. Nothing here imports JPA or Spring Data (ArchUnit enforces this).
 * identity.domain.model.AppUser is the template.
 */
package sg.nus.carelink.careplan.domain.model;
